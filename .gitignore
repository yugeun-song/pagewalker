tags
.tags*
.netrw*
*.swp
*.swo

# clangd compile database: generated per kernel tree with `make clangd`, carries
# absolute machine-specific paths, so it is not committed (the .clangd config is).
compile_commands.json

kernel/*.o
kernel/*.ko
kernel/*.mod.c
kernel/*.mod
kernel/*.order
kernel/*.symvers
kernel/.*.cmd
kernel/.tmp_versions/
kernel/modules.order
kernel/Module.symvers
kernel/*.o.d

user/pagewalkerctl
user/selftest
user/*.o
user/*.d

kernel/compile_flags.txt

cscope.files
cscope.out
cscope.in.out
cscope.po.out

.DS_Store
Thumbs.db
*~
